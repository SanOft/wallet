# Parking lot

Known gaps, each found by review rather than imagined, each with the reason it
was not fixed on the spot.

## Two bars, and why the difference matters

This started as a portfolio project and the disclaimer in `spec.md` is still
accurate: no real money moves through it, and there is no licence. But the
architecture was built to the standard Appendix A states — *"prove that today's
architecture can absorb real payments without a rewrite"* — and the intent is a
product, not a demonstration.

So the list below is triaged against the **product** bar, not the demo one. The
distinction is not academic: several entries were reasonable to defer while the
only user was a reviewer, and stop being reasonable the moment a real person's
identity or money is in the system.

| Tier | Meaning |
|---|---|
| **A** | Blocks the first real user. Not "should fix" — the system is unsafe or broken without it. |
| **B** | Blocks running unattended, or at more than one instance. Fine while someone is watching; not fine as a service. |
| **C** | Quality and maintenance. Genuinely September. |

**Rule while the architecture is frozen (runbook §5):** write it down, then go
back to the task at hand.

---

## Tier A — blocks the first real user

| # | Raised | Gap | Why it is A, and what closing it takes |
|---|---|---|---|
| P-4 | Day 2 | **The API connects as a superuser that owns its own tables.** The append-only trigger, the treasury guard and the balance constraints all hold against ordinary DML, and none survives `ALTER TABLE … DISABLE TRIGGER`, `CREATE OR REPLACE FUNCTION`, or `session_replication_role = replica`. | The ledger's guarantees are the product. A compromised application process — the most likely thing to be compromised — can currently rewrite them, which is why the migration header claims only that "a bug in service code cannot corrupt the ledger", not that nothing can. Closing it is a runtime role that owns nothing and holds only `SELECT`/`INSERT`/`UPDATE` on the tables it needs. Needs the hosted database (T-2.6). **Do the credential rotation in the same pass:** the deployed role is `wallet_owner`, its password has been exposed once outside the secret stores, and closing this item already means editing `DATABASE_URL` in all three places — `.env.neon`, the platform environment, and the two repository secrets. Rotating separately edits the same three twice. **The role now exists as a file, and is proved rather than described.** `prisma/runtime-role.sql` creates `wallet_runtime`, owning nothing, and `runtime-role.test.ts` executes that file — not a copy of it — and then runs registration, the demo mint, a transfer and a history read on that connection, because a role locked down enough to be safe and too locked down to run is an outage rather than a fix. Determining the grants empirically was the work: `SELECT`, `INSERT`, `UPDATE` everywhere, `USAGE` on the sequences for `ledger_entries.seq`, and `DELETE` on **only** `idempotency_records` and `transfers`, which is where `TransferService`'s expiry sweep actually deletes. All five escalations this entry names are refused, and each is asserted on `42501` specifically rather than on "it threw". A note for whoever reads the trigger and assumes it is what protects the ledger: under this role `DELETE FROM ledger_entries` never reaches it, because Postgres checks privilege first. **What remains is operational and yours:** run the file against Neon with a generated secret, then point `DATABASE_URL` at `wallet_runtime` in the three places, rotating `wallet_owner` in the same pass. Migrations keep using the owner through `DATABASE_URL_UNPOOLED`. |
| P-11 | Day 2 | **`trust proxy` is asserted only in a comment, and the hop count is now genuinely uncertain.** Off the load balancer, `req.ip` and `req.secure` are client-controlled. ADR-0009 added a second proxy in front of the first, and `app.set("trust proxy", 1)` encodes a count that was chosen for the old chain. | Every rate limit keys on `req.ip`, so a wrong count either buckets every caller into one budget or trusts a forged header. What each vendor puts in `X-Forwarded-For` is a claim about runtime behaviour and has to be measured, not reasoned about — T-6.3's production smoke is where it gets settled, together with the test pinning both topologies. **Half of it is now measured, and the result is not reassuring.** `proxy-hops.test.ts` pins what Express does with a given count, because that half never needed production: hops are counted from the right, nearest the application, so the number is not "how many proxies exist" but "how many may be believed" — and one too few silently yields a *proxy's own address*. Measured: with `trust proxy: 1` and a chain of `client, cdn`, `req.ip` resolves to the CDN. ADR-0009 serves the API through the web origin, so a production request arrives as at least `client, vercel`. **The likely failure is therefore the first one this entry names — every caller in one budget — and it is an availability failure that reads like a security control working.** A test on the real application demonstrates it: twenty-four distinct callers behind one proxy exhaust a shared allowance. The count is deliberately *not* changed here. Whether the real chain is two hops or three is still a claim about vendor behaviour, and guessing high is the other failure — a forged header, trusted. What is removed is the second unknown: when T-6.3 measures the deployed origin, the only open question is a number, and the table in that file already says what each number means. |

---

## Tier B — blocks running unattended, or at more than one instance

| # | Raised | Gap | Why it is B |
|---|---|---|---|
| P-22 | Day 6 | **Rate limiting and the lookup counter are in-process.** `express-rate-limit`'s default store and the `Map` in `routes/recipients.ts` both live in one Node process, so the limits are per-instance and reset on restart. | Render's free tier runs one instance, so this is correct *today* and wrong the moment there are two — the effective limit silently multiplies by the instance count. It also resets on every deploy and every cold start (§20.3), which is a scheduled bypass rather than an edge case. A shared store is the fix, and it should move both counters at once. |
| P-27 | Day 6 | **A sleeping instance refuses requests rather than queueing them.** Render's free tier spins down after inactivity, and while it wakes, its edge answers `404` with `x-render-routing: no-server`, `text/plain`, and no request id — observed four times in a row on `POST /api/auth/register` between two successful calls. The first person to open the app after a quiet period gets a hard failure, not a slow load. | Nothing in the API can fix it: the request never reaches the process. The options are a paid instance, an external pinger, or a client that distinguishes "not running" from "broken" and retries — and only the last is free. The smoke test already does that, by requiring the `x-request-id` the service sets itself; the PWA will need the same rule when it exists (F1). Until then it is a demo-grade availability floor, and worth stating plainly rather than discovering during a demonstration. |
| P-13 | Day 3 | **Registration has a timing oracle.** A free number does two INSERTs, a taken one a failed INSERT: means 56.8 ms against 46.8 ms, minima fully separated, 60% classification accuracy. | FR-1.5 exists so an attacker cannot walk a number range and learn who banks here. With real customers that list has value, and the control is currently satisfied for body and status only. Login's version was closed by making the write unconditional; registration has no such lever, so closing it means padding responses to a fixed budget — a decision that touches every write path and should be made once. **Re-measured after #64, because the original figures were taken on day 3 and the registration path has changed since.** Twenty samples per arm, interleaved so machine drift hits both equally, each request from its own address so the 20-per-IP register limit cannot answer before the work happens — the same precaution `auth.test.ts`'s timing harness documents, where omitting it once left the constant-time defence deletable with all tests green. Four runs: mean gap 11.2-13.5 ms, ratio 1.18-1.22, minima fully separated 4/4. **The ratio is unchanged** — 1.20 against the 1.21 recorded — so the oracle has neither grown nor closed. What did change is the confidence figure: a midpoint classifier on a single sample scores 73-88% across the four runs, mean ~80%, against the 60% in this entry. Not a regression, since the underlying gap is the same; the earlier number understated how separable the two arms are. An attacker walking a number range does not get one sample per number, either — they can take as many as the rate limit allows, and the means separate long before the minima do. **Two structural equalisations have now been implemented and measured, and neither closed it — the entry stays open.** First attempt: give the refused path the same two inserts and roll them back. Gap fell 12.1 to 8.7 ms and the ratio 1.20 to 1.17, but single-sample accuracy went 79.8% to 78.7%, which is inside the noise. Reverted rather than shipped: it added two attacker-triggerable inserts per refusal for a benefit that could not be measured. A diagnostic run then committed those discarded writes instead of rolling them back and accuracy fell to 53-60%, near chance, which pointed at durability — **and that inference was too quick**, as the second attempt showed. Second attempt, shipped: a refusal writes and commits an `auth_attempt`, and the session is issued inside the registration transaction so both outcomes commit exactly once. Forty samples per arm, three runs: gap 8.5-9.8 ms, ratio 1.17-1.19, accuracy 73-83%. Commits are now 1:1 and the oracle is still there, because the accepted path writes four rows (user, account, attempt, refresh token) against the refused path's one. **The residue is the intrinsic cost of creating an account**, and closing it inside "equalise the work" would mean the refused path writing four equivalent rows — ballast under a different name. The two changes were kept because each stands on its own: an unauthenticated endpoint that creates accounts previously left no trace when it refused, and a user could briefly exist without the session meant to accompany it. Neither should be read as closing this. What remains is a choice between padding the residual, real ballast, and accepting the leak with the rate limit as the stated control. |

---

## Tier C — quality and maintenance

| # | Raised | Gap | Why it can wait |
|---|---|---|---|
| P-18 | Day 4 | FR-4.7's maximum and FR-6.1's WEB per-operation limit are the same number, so `limit.per_operation` is unreachable on the web channel. | A product question about fraud exposure, not an implementation one. Recorded so nobody "fixes" the ordering and expects different behaviour. **The half that was a note is now a test; the half that is a product question still is.** "Recorded so nobody fixes the ordering" is a job prose does badly, so it is enforced instead: `reachable-limits.test.ts` pins the equality between FR-4.7's maximum and the web cap, and a test in `day5.test.ts` pins the consequence — an over-large web transfer answers `400 money.above_maximum`, never `422 limit.per_operation`. Two guards because they fail to different changes: lowering the cap moves the constant, and swapping `#assertAmountIsSane` with `#assertWithinLimits` leaves both constants untouched while flipping what a caller sees. Both mutation-proved. Measuring it also corrected the entry: a **third** refusal comes first. `#assertStepUp` runs before the transaction, so an amount that large is answered `STEP_UP_REQUIRED` unless the password is supplied, and only then is the amount examined at all. The USSD cap sits below the maximum, which is what keeps the limit check from being dead code — also asserted, since a rule enforced on no channel is worse than no rule. **Still open, and still yours:** whether the web cap should sit below FR-4.7's maximum is a question about fraud exposure, and nothing here answers it. |

---

## Beyond the code

Closing every entry above still would not make this a product that handles real
money. Recorded here so the list is not mistaken for a complete one:

- **A licence and a legal entity.** §A.5 is explicit: a real integration with
  Payme, Click or Uzum requires a contract and a registered company.
- **KYC and AML.** §3 lists them as frozen non-goals. A product cannot.
- **Monitoring and alerting.** `fatal` reaches a log stream; nothing pages
  anyone. The reconciliation job exits non-zero and nothing watches it.
- **Backup and recovery policy.** A ledger needs a retention decision and a
  tested restore, not whatever the hosting tier defaults to.
- **An incident runbook.** What to do when reconciliation fails is currently
  "someone has to look", which is not a procedure.

---

## Closed

| # | Raised | Gap | Resolution |
|---|---|---|---|
| P-33 | B6 | **The global IP rate limit would throttle a real USSD gateway on its first busy hour.** `globalRateLimit` allows 300 requests per 15 minutes per address, and a gateway is one address serving every subscriber on the network — a four-step session is four requests, so 75 sessions exhausts the budget for everyone. Worse than the throttling: a refused request is answered with the §12.3 JSON envelope, which a gateway renders on a handset. | **The entry's own reason for waiting was wrong, which is why this was fixed rather than deferred again.** It says "not fixed now because there is no gateway" — but the gateway route exists and is live: `POST /api/channels/ussd`, authenticated by `x-gateway-secret`, mounted at `app.ts:179`, and `globalRateLimit()` sits app-wide at `app.ts:135` in front of it. So this was a present defect in a shipped route, not a hypothetical about a future one. Closed both halves. The callback is now metered by `ussdGatewayRateLimit`, keyed on the subscriber's number from the form body — which is why it mounts *after* `express.urlencoded` — at forty requests a quarter hour, ten four-step sessions for one person; the global budget skips this path, since leaving it would put the address limit back in front and the first limiter to refuse decides what the subscriber sees. A callback carrying no number falls back to the address, so a malformed body cannot buy an unmetered budget. And a refusal now ends the session the way every other ending does: `END Xizmat band…`, 200, no JSON. Both halves mutation-proved — keying on the address fails the fifty-subscriber test, and removing the custom refusal puts `RATE_LIMITED` back on the handset. |
| P-26 | Day 6 | **The production smoke test writes to the production database and cleans up nothing.** T-6.3 registers two accounts, mints demo funds and completes a transfer on every deploy. The numbers come from the unassigned `+998 33` range so they are recognisable, but nothing removes them. | **Resolved as accepted, deliberately, after two findings changed the question.** First: the rows cannot be deleted. Probing it — register, mint, then attempt the cleanup — gives two refusals in series. `DELETE` on the user is rejected by `transfers_initiatedBy_fkey`, because a transfer records who pressed send and that reference outlives any wish to forget them; reaching past it to the entries is rejected by the append-only trigger with `23001`. So "an endpoint that does not exist or a scheduled job" was never the answer: this is I-3 working as ADR-0001 intends, and any cleanup would have to break the guarantee the product is sold on. Second: **there is no reporting layer to exclude them from.** The only `count()` calls in `apps/api/src` are FR-10.3's top-up allowance and FR-6.3's velocity check, both per-user, and both are *correct* to include a smoke account — its own limits should apply to it. Building a metrics layer in order to filter one would be solving a problem that does not exist yet. What the accumulation actually distorts is the treasury: every deploy mints `DEMO_TOPUP_AMOUNT`, 1 000 000 so'm, and the treasury's negative balance is by definition the total minted, so it counts demo money issued to a test runner alongside money issued to people. That is one top-up per deploy and it is countable. **Two rules follow, and they point in opposite directions.** Any future metric of users, accounts or issuance must exclude the `+998 33` range — the rule is recorded here because the code that would honour it does not exist yet. And the invariants must **never** exclude it: a smoke account's ledger has to balance like anyone's, and filtering it out of `reconcile` would build exactly the blind spot the job exists to prevent. |
| P-39 | #63 | **A CI run could fail with every test passing.** `test (24)` went red on #63 with 25 files and 298 tests passed and `Errors 1 error`: `ReferenceError: cancelAnimationFrame is not defined`, from a Redux Toolkit timeout callback attributed to `ussd-simulator.test.tsx`. `test (22)` passed the same commit. | Diagnosed by reading Redux Toolkit's source rather than guessing. `createRafWithFallbackTimer` schedules **two** things per notification — the animation frame and a 100 ms `setTimeout` — and whichever fires first cancels the other with `cancelAnimationFrame`. Vitest tears the jsdom environment down between files, so the fallback timer can fire into a world with no such global. The CI stack said `Timeout.callback`, which is the fallback rather than the frame, and that is the detail that identified it. **My first recommendation was wrong and is worth recording:** an injectable store would not have fixed this. There is no disposal API on a Redux store to bind the pending callback to, and dispatching in `afterEach` would schedule a *new* one rather than cancel the old. Closed instead by `autoBatch: { type: "tick" }`, which uses `queueMicrotask` — it drains at the end of the current task, before anything can be torn down, and schedules neither a frame nor a timer. The failure is not made less likely; it is made impossible, because nothing is left pending to fire. Sound in a browser too: notifications still coalesce within a task, which is where a burst of dispatches happens; what is given up is coalescing across tasks inside one frame, for lower latency. Proven by `store-batching.test.ts`, which asserts that a batched action schedules no frame and no timer — it fails under `raf` with "scheduled an animation frame: called 1 times" and passes under `tick`, so the assertion is load-bearing rather than decorative. Its control first asserted that notification was *deferred* and was wrong, since the subscriber is called more than once per dispatch; it now asserts only that notification still happens. **Residual: the flake itself was never reproduced**, so this fixes the mechanism the stack trace names rather than a symptom observed to disappear. |
| P-12 | Day 2 | Spec ER drift: §9.1 showed `USER ||--|| ACCOUNT` while the schema allows one account per currency, and `TRANSFER \|o--\|\| IDEMPOTENCY_RECORD` was not modelled. | Both corrected, and a systematic diff of the diagram against `schema.prisma` found four more the entry had not: **`RatesSnapshot` was missing entirely** (an eighth table, added at B4 by P-30); `USER.tokensValidAfter` was missing (P-16); `LEDGER_ENTRY.seq` was missing (P-21); and `AUTH_ATTEMPT.subject` was missing — the keyed digest that makes an unregistered number back off identically (FR-2.3), so the diagram omitted the mechanism a reader would go there to understand. `TRANSFER` also lacked `initiatedBy` and still marked `idempotencyKey` as globally unique, which P-8 changed to `(initiatedBy, idempotencyKey)` — verified in `20260827200000_scope_idempotency_to_user/migration.sql`, which drops the old index rather than leaving both. The idempotency relation now points at `USER`, because that is the only foreign key there is: a record survives a request that produced no transfer, so the line to `TRANSFER` described a constraint the database never had. Three downstream "7 tables" claims were stale as a result; each is a record of what day 2 delivered, so the count is kept and the eighth table's arrival noted rather than history rewritten. **The lesson is the method, not the fix:** the entry recorded the two drifts somebody noticed, and enumerating both sides found four more in a minute. A diagram checked by reading is a diagram that drifts. |
| P-29 | F2 | **The contrast test checked a list, not the stylesheet's actual use.** `contrast.test.ts` walked the eight pairs someone wrote down in §13.2.2. F2's failure notice used `--color-danger` on `--color-surface-sunken`, a ninth pair in neither, so a component shipped a combination nothing measured — caught by looking at a screenshot, which does not scale. | **The repair this entry proposed would have covered about an eighth of what ships, and was measured before being written.** Pairing each element's own `color` with its own `background` finds nine elements in `src`; sixty-seven set a foreground and inherit their background from an ancestor — so that repair would have missed precisely the inherited-background case that caused the defect. Closed by not pairing per element at all: the test takes every foreground token any component uses and every background token any component uses, and checks the whole product. Stronger than pairing, because it holds however components are nested and stays true when one is moved inside another. The `on-*` convention §13.2.2 already states is the one exception — a background with a counterpart pairs with that counterpart alone, without which the product is meaningless (`--color-text` on `--color-primary` measures 2.96 and no component puts it there). Thirteen pairs derived where nine were listed. **It immediately found a tenth and eleventh:** `--color-neutral`, used as text for QUEUED rows and disabled controls, published a `—` in the contrast column — the table said out loud that it was unmeasured, and it shipped anyway. It measures 4.97/5.31 on the page and 4.76/4.85 on a sunken surface: passing, and the closest any pair in this system comes to failing NFR-4. Two mutations prove the check is live — a component given a `bg-(--color-warning)` fails five newly-enumerated combinations, which is this entry's scenario exactly, and a lightened `--color-neutral` fails both of its pairs. A control asserts the scan found sources and tokens, because an `it.each` over an empty derived list passes silently and would have turned the whole file green. |
| P-37 | #57 | **Six test files minted phone numbers with their own `Math.random()` instead of `uniquePhone`.** Kept apart from one another only by the convention that each picked a different operator prefix — and `history.test.ts` used `+99893`, the exact space the shared helper occupied before #57 moved it, so the convention had already failed once unnoticed. | Five converted; every call site untouched, because each file already imported from `helpers.js` and none asserted on a prefix, so only the definition and the import changed. The win is not fewer lines: the numbers are now deterministic per file, which brings them under `phone-uniqueness.test.ts` — that guard enumerates every test file, so these five went from *probably* distinct to an invariant that is checked and names the offenders when it breaks. **`seed-demo.test.ts` deliberately keeps its own, and now says why**: its numbers do a second job, since `+998 33` is unassigned to carriers and a row it leaves behind must be recognisable as a fixture rather than as somebody's account — that file deletes rows *by phone number*, and its own header records what happened when one of them named a real account with a balance and three transfers. `uniquePhone` returns `+9989…`, which looks like a customer, so converting it would have traded a safety property for consistency. The duplication was the defect, not the arithmetic: a shared helper existed and drifted because nothing made using it easier than writing a new one. |
| P-38 | #57 | **A varying set of web tests failed under load.** Recorded as one flaky test in `readCache.test.tsx`; that was wrong twice over. | Reproducible, which is what made the diagnosis possible: `for i in 1 2; do (yarn vitest run --coverage=false &) ; done` failed 2/2 and passed 6/6 without contention. Under contention it was never one test — four or five different ones each run, all expiring between 3 200 and 5 200 ms, including two added by #57 itself. Nothing in the path waits on a timer, so the only variable is CPU. **The first fix was wrong and is worth recording.** Raising `asyncUtilTimeout` from 3 000 to 10 000 made things *worse* — five failures instead of four — because it lifted the assertion budget above Vitest's own 5 000 ms `testTimeout`, so the test died before `waitFor` could report what it was waiting for, turning informative failures into `Test timed out in 5000ms`. The real defect was that the two budgets were unordered, and the previous 3 000 worked only by accidentally sitting under the 5 000 default. Closed by ordering them deliberately: `testTimeout: 20_000` in `vitest.config.ts`, above `asyncUtilTimeout: 10_000`, each carrying the reason. The order is the fix — the budget that fires first must be the one that knows what it was waiting for. Proven both ways: 279/279 on both suites under the contention that failed 2/2 before, and a deliberately broken assertion still fails with the expected text rather than a bare timeout. The chain is what grew: the wizard gained a recent-recipients query and accounts gained the daily allowance, so a budget sized for last month's chain turns load into false failures. |
| P-36 | F4 | **§13.5's recent-recipients quick pick is not implemented.** Step rule 1 asks for one beside the lookup; the wizard has only the number field. Reported by a user, who reasonably expected people they had already paid to still be there. | Closed together with P-32, since both were one change to the same two responses. The wire change first: `historyItemSchema`'s counterparty gained `phone`, and it is `null` on an incoming transfer — the disclosure is narrow because a recipient is somebody the user already typed a number for, and a *sender* is not. `RecentRecipients` derives the list from the five rows the home screen already holds rather than from a new endpoint or a contacts table: a list built from transfers cannot drift from them, and nothing has to be deleted when a person is forgotten. Three entries, distinct, newest first; a failed transfer still counts, because somebody whose payment was refused is more likely to be trying again. Picking fills the field **and runs the lookup** — a shortcut that merely filled the box and enabled Continue would be a way around FR-4.9, since a number from this device's history is not proof the account still exists under that name. The number is shown beside the name deliberately: FR-4.6 makes masked names non-unique, so two people can render as the same label. Four mutations, all caught. The direction guard needed a row the server does not produce — an incoming transfer carrying a phone — because every honest incoming row already has `null` there, so the guard would otherwise have been proven by the masking rather than by itself. |
| P-32 | F4 | **The transfer screen cannot show the remaining daily allowance §13.5 asks for.** No endpoint returns it, and computing it on the client means summing today's outgoing transfers from a paged history — right until somebody exceeds one page in a day, then silently understating what they have spent. F4 shows the per-operation cap and the balance instead. | Closed by serving it from `GET /api/accounts`, as the entry proposed, but **not** waiting for the limits to move to the database as the entry proposed: that was sequencing to stop the client learning the number twice, and serving it means the client never learns it at all, so the reason to wait had already been satisfied. What made this safe is that `spentOnChannel` was extracted rather than written: the display and the refusal now read one definition of the rolling 24-hour window (FR-6.1 — a window, not a calendar day), so the figure on screen cannot disagree with the check inside the transaction. Enforced as well as displayed, for the same reason the balance is: a client that can see the rule and still lets Send be pressed spends a round trip to say no. An unknown allowance means *not blocked* — the server holds the real gate, and a request in flight must not refuse a legitimate transfer. Shown as an em dash while it loads, never as the full limit. The first attempt returned a 500: `moneySchema` is an inbound parser that transforms to `bigint`, and `JSON.stringify` refuses BigInt — a response schema needs `z.string()`. Four mutations, all caught. Still local, and correctly: the *per-operation* cap is a constant the client reads from `CHANNEL_LIMITS`, and moving that to the database remains §12's open work. |
| P-24 | Day 6 | **`requestId` was caller-controlled.** Any well-formed inbound UUID was honoured, echoed and logged, and `x-request-id` is on the CORS allowlist — so a caller could collapse many requests onto one id, or reuse one it had seen. §17.1 answers Repudiation with these logs. | Two ids now. `requestId` is unchanged and still the caller's: echoed in the header and the §12.3 envelope, because a client correlating its own call is why the header exists. `traceId` is minted per request, never read from anything inbound, and is what every log line is keyed on — a correlation key the accused chooses is not corroboration. Not put on the wire: it is for whoever reads the logs, and a second header would be a value nothing consumes. Three mutations, all caught. **The serializer took two attempts:** pino-http passes a wrapper rather than the Express request, so `req.traceId` was simply absent, and the wrapper has enough of the same fields — `url` resolves — that only the property nobody else sets came back undefined. It reads through `req.raw` now. |
| P-25 | Day 6 | **Per-IP limits in a CGNAT market.** Twenty auth requests per fifteen minutes, keyed on address, in a country where carriers put whole subscriber pools behind one — so it was never twenty attempts by one person, it was twenty sign-ins for everybody on that NAT. | Split into two budgets, because the endpoints limit different things. Registration still counts every request including successes, since *successful* account creation is the thing P-20 caps. Login counts **failures only** (`skipSuccessfulRequests`), which separates the two populations by the property that actually distinguishes them: legitimate users mostly succeed, attackers mostly fail. A subscriber who signs in correctly now costs the shared address nothing, however many of them there are. Fifty rather than twenty because these are failures. **The proposed fix was not taken.** This entry suggested keying on the phone number as well as the address; that was rejected for two reasons. FR-2.3's backoff already counts per number (P-15), so a second per-account counter is one rule in two places — P-34's mistake. And keying on `(address, number)` would give a spray attacker a fresh budget for every account tried, which is exactly what the address budget exists to bound: it would read as a tightening and be a loosening. Three mutations, all caught — but only after the spray test was rewritten: `AUTH_LOCKED` and `RATE_LIMITED` are both 429, so its first version passed on backoff responses the limiter never produced, and removing the budget entirely left it green. |
| P-35 | P-5 | **Two flaky web/API tests, first seen as one unreproduced `254 passed, 1 failed`.** | Both caught by running `yarn verify` repeatedly and keeping the output, which is what the first attempt failed to do. **The §11.7 simulator session timed out at 5000 ms** — five full request/response cycles, measured at 5152 ms, green alone and red inside `verify` where the API suite has just run and coverage is attached. `delay: null` on the typing was tried first and did not move it, which is what identified the round trips rather than the keystrokes as the cost; the test now declares a 20 s bound and says why. **And `ledger-invariants.test.ts` collided on phone numbers** — its helper used `Math.random()` over seven digits, which is the hazard P-17 recorded about `uniquePhone()`, repeated in a new file. A counter-based generator now lives in `test/helpers.ts` and the three suites that each rolled their own use it. Four consecutive clean `yarn verify` runs. |
| P-5 | Day 2 | CI actions were mutable tags rather than SHAs; no `permissions:` block; no `timeout-minutes`; and only Node 22 was exercised although `engines` allows `^22 \|\| >=24`. | All four, in one pass, across both workflows — `deploy.yml` included, since it carries the deploy hook and the production database URL and is the more valuable target of the two. Actions pinned by commit SHA with the version as a trailing comment; `permissions: contents: read` stated rather than inherited, because a repository default of write would hand every third-party action a token that can push to `main`; every job bounded, against a six-hour default. Node 24 is now exercised by a matrix, which costs no wall-clock time on a public repository and turns half of `engines` from a claim into a check. **Pinning needed its other half:** a SHA with no updater is a freeze, so `.github/dependabot.yml` was added — the comment claiming Dependabot kept the pins current was written before the file existed, which is the kind of comment E2 is about. `test/workflows.test.ts` asserts all of it; five mutations, all caught. |
| P-3 | Day 2 | CI pinned Postgres by floating tag (`postgres:17-alpine`), so a green run was not reproducible months later. | Pinned by digest, with P-5 as that entry predicted. It matters more here than for a typical dependency: the CHECK constraints, the triggers and the deferred constraint trigger this suite leans on are the *database's* behaviour, not ours, so an unpinned image means the invariants are verified against whatever image was current that morning. |
| P-2 | Day 2 | **`Account.balance` could drift from `sum(ledger)` with nothing detecting it in real time.** I-4 was a daily job (§20.4), so a drift at 09:00 stayed invisible until the next morning — and the transfers built on the wrong number had happened by then. | Refused at COMMIT, in the deferred trigger that already held I-1, I-2 and I-6. The account's snapshot is compared with the newest entry's `balanceAfter` rather than with `SUM(amount)`: the chain check below has just established they are the same number, and summing instead would cost the treasury an aggregate over its whole history on every demo top-up. A day of wrong balances became a transaction that cannot commit. The daily job stays, for rows written before this and for anything that reaches the table outside a transfer. |
| P-21 | Day 5 | **`balanceAfter` was validated by nothing** — not the CHECKs, not `assert_transfer_balanced`, not `reconcile` — while §9.2 sells it as making an audit O(1). | All three now check it. The COMMIT-time half verifies each new entry against its predecessor on the same account, which makes the whole chain correct by induction and costs O(1); `reconcile` reports breaks in rows the trigger never saw, and `isHealthy` counts one as unhealthy even though no balance is wrong when it happens — an audit column that lies is worse than none, because the reconciliation built on it reads from the source it is meant to police. **This needed a schema change to be possible at all:** `createdAt` defaults to the transaction timestamp, so every entry one transaction writes shares it and "the previous entry for this account" was not well defined — a self-transfer, which nothing forbids, is exactly the tie. A `seq BIGSERIAL` settles the order. The migration also repairs existing rows once, with the append-only trigger suspended and the counts reported: `balanceAfter` is derived from the amounts, so recomputing it restores a cache rather than rewriting history. Three mutations against the installed function, all caught — after the harness itself was fixed, having first weakened the function in the development database while running the tests against the test one, so every mutation "survived". |
| P-17 | Day 3 | **The DB-backed suites cleaned up nothing and could skip silently.** No teardown, and `describe.skipIf(!hasDatabase)` guards twenty-two blocks across nine files — without a `DATABASE_URL` they disappear and the run still reports success. The shared development database had reached 2 804 users, 2 409 transfers and 4 478 ledger entries, and an I-4 drift of 2 300 000 tiyin had settled into it and persisted across every later run, turning four tests red against unmutated code. | Both halves closed in `test/global-setup.ts`, which runs once per invocation. **Fails closed where it matters:** no database with `CI` set is now a thrown error, because a missing variable there means the workflow is broken, not that the machine has no Postgres. **Stays loud where skipping is correct:** on a developer machine it warns that nine suites did not run and the summary will still say passed — correct to skip and fine to say nothing about are different claims. **Truncates once per run**, with `session_replication_role` for the one statement, because the ledger rejects TRUNCATE by trigger and that guarantee is not something to weaken for a test. Measured: 2 985 rows before, 183 after, and 183 again on the two runs following — bounded rather than merely smaller. The reset's own first version silently did nothing, because `setupFiles` run *after* `globalSetup` and `TEST_DATABASE_URL` was not loaded yet; a green suite and 181 new rows were the only evidence, which is the failure mode this entry is about, reproduced one level down. Both branches proven by hiding the env file: CI exits 1, local exits 0 with the warning. **Correction, added when it recurred:** this entry's phone-number half was only half fixed. `uniquePhone` became counter-based *within* a file, but files were kept apart by `Math.random()` over three digits — twenty-one files in nine hundred buckets, so roughly one run in five minted a duplicate and some unrelated suite failed registration with a `400`. It surfaced as `test (22)` red and `test (24)` green on identical code. The separator is now derived from the test file's own path, and `phone-uniqueness.test.ts` asserts no two files collide, so the property is checked rather than sampled. |
| P-10 | Day 2 | **`noRestrictedImports` was not wired**, so §8.2's one-way dependency rule was held by review. Behaviourally obeyed, and blocked on `apps/web` having code — one workspace cannot import another that is empty. | Wired as three per-layer overrides in `biome.json`. The wiring had the failure mode the rule exists to prevent, which is why it was tested rather than trusted: the first version used `**/apps/api/**` alone, and that does not match `../../api/src/domain/AuthService.js` — the relative escape anyone crossing workspaces would actually write. `tsc` compiles that import with no complaint, so nothing would have caught it. Measured against Biome 2.5.10 with five probe files: four violations, one legitimate import that must stay quiet, all five correct only after `**/<app>/src/**` was added. `packages/shared/test/dependency-direction.test.ts` pins all three forms, because a group that matches nothing looks exactly like one that works. |
| P-19 | Day 2 | **No `AccountService`.** §8.3's C3 diagram puts balance, history and lookup in the domain with both the REST adapter and the USSD adapter pointing at it; `routes/accounts.ts` and `routes/recipients.ts` queried Prisma directly and FR-4.9's cap lived in the route. | Extracted once B6 had defined what the second channel needs, which is what this entry was waiting for — building the service against one consumer would have been guessing at the other. `AccountService` owns the balance read, caller-ID resolution and FR-4.9's lookup as one thing: the budget, the exact-match query and the masking together. Masking moved inside it so a full surname cannot leave the domain even if a future adapter forgets (FR-4.6) — both existing callers already masked, the point is that the third cannot fail to. History stays with `TransferService`, deliberately: it owns the rows, the cursor and the ownership predicate, and moving the read away from the writer would split one table's rules across two services to satisfy a diagram. |
| P-34 | B6 | **FR-4.9's lookup window existed twice** — inline in `routes/recipients.ts`, and as a class in `adapters/ussd/window.ts`. Same twenty, same hour, two implementations. | Collapsed into `AccountService` with P-19, as that entry predicted. `SlidingWindow` moved to the domain and there is now exactly one instance, constructed once per process — a second would be a second budget. Proven rather than asserted: four mutations against the service, and three of them (cap removed, budget raised out of reach, an unregistered number answered instead of refused) turn **both** the web suite and the USSD suite red from one edit. The fourth — the window shrunk sixtyfold — is caught only by the web suite, which injects a clock, and that is now sufficient: there is no second window left to drift from the one it pins. Closing this also exposed a real gap and closed it: the USSD channel's cap had no end-to-end test at all, so a lookup with no budget would have passed — on the one disclosure §11.7 puts before the PIN. |
| P-23 | Day 6 | **§17.1 mitigated "token theft via XSS" with a CSP on the wrong origin.** The access token lives in memory on the web origin (FR-2.4); the CSP was a helmet header on the Render API. A policy on one registrable domain constrains nothing that can reach a variable on another, so the control the threat model claimed was deployed on the host that did not need it. | Closed by a document-origin policy in `apps/web/vercel.json`: `default-src 'none'`, no `'unsafe-inline'`, no `'unsafe-eval'`, plus `frame-ancestors`, `base-uri` and `object-src`, which `default-src` does not cover. The surprise was that the inline styles cost nothing — almost every component sets `style={{...}}` because §13.2 bans raw values, and that looks like it forces `'unsafe-inline'`; React writes them through CSSOM, and CSP governs the parser. Measured rather than assumed: the whole application served behind this exact header renders with its styles applied and reports no violations, including the service worker and a POST. Pinned by `apps/web/test/csp.test.ts` — seven mutations, all caught — because a policy that can be weakened without anything failing is decoration. `X-Content-Type-Options`, `Referrer-Policy: no-referrer` and a `Permissions-Policy` denying the device APIs this application never asks for were added in the follow-up pass, so the document origin now matches what helmet has given the API since B5. All four verified served and parsed against the real bundle — `document.featurePolicy.allowsFeature("camera")` returns false, which a malformed policy would not, since browsers drop those silently. **Residual: delivery in production is unverified.** `vercel.json` still rewrites to `set-me-at-t-6-1.invalid`, so nothing serves these headers yet; T-6.1 is where it becomes real, and one `curl -I` against the deployed origin settles it. |
| P-1 | Day 2 | §12.3 had no code for a request that reached no route, an unparseable body, or an oversized one — all three left the API as HTML or as a *retryable* `INTERNAL 500`. | `NOT_FOUND`, `MALFORMED_BODY` and `PAYLOAD_TOO_LARGE` added to the catalog and to `packages/shared`, with a terminal 404 handler and body-parser mapping. |
| P-6 | Day 2 | gitleaks was named by the spec three times and existed nowhere. | Added to CI pinned by image **digest**. It found two matches on its first run: the `.env.example` placeholder, allowlisted by path with the reason written down, and a hard-coded test secret, which was removed rather than allowlisted. The historical commit is allowed by SHA, so a real secret in that same file tomorrow still fails. |
| P-7 | Day 2 | Coverage was an aspiration with no instrument. | Measured and gated in both workspaces. The domain clears 90% on lines, statements and functions; branches sit at a measured floor with the reason recorded rather than the number quietly lowered. `packages/shared` went from 31% to 97% once it had its own tests — which is a large part of why two masking bugs had shipped. |
| P-9 | Day 2 | §12.2 mandated a 400 for a missing `Idempotency-Key` and §12.3 had no code for it. | Resolved as `VALIDATION_ERROR` with `path: ["Idempotency-Key"], code: "field.required"`, on both money endpoints, each with a test. |
| P-14 | Day 3 | **`SameSite=Strict` and the §20.1 topology could not both hold.** Vercel and Render are separate registrable domains, both on the Public Suffix List, so the refresh cookie could never attach — invisibly, because supertest and local development each use one host. | Closed by ADR-0009: the API is served through the web origin with a Vercel rewrite, so `/api` is same-origin and FR-2.4 stands unamended. Two consequences handled with it — CDN caching disabled in `vercel.json` *and* `Cache-Control: no-store` from the API, because a cached balance is one user's shown to another; and the proxy chain gained a hop, which is why P-11 now blocks on the production smoke test rather than on a test topology. |
| P-28 | F1 | **S-5's timing assertions had stopped measuring logins.** They looked flaky — 1.615 against a 1.5 bound under load — and the flakiness was a symptom. | The auth rate limit added at T-6.4 (20 per address per 15 minutes) answered most of each batch with `429` before any hashing, so the minimum of the batch was the fastest refusal and both arms compared two identical rejections. Measured: thirty logins from one address returned `{"401": 20, "429": 10}`. With the limiter in the way, deleting the constant-time defence entirely left all 27 tests green. Fixed by sampling from a distinct address per request. The numbers are now the ones `crypto.ts` describes — ~44 ms per arm against ~9 ms with the defence removed — the observed floor fell from 1.05–1.38 to 1.00–1.12, and the reintroduced leak measures 4.7. The bound stays at 1.5: it was never too tight, and raising it would have been the change that keeps the test green while removing what it detects. |
| P-15 | Day 3 | **FR-2.3 lockout was not implemented.** `AuthAttempt` rows were written but nothing counted them; `AUTH_LOCKED` existed in the catalog, the status map and a test, for a code the application could never emit. | Implemented as the exponential backoff FR-2.3 actually specifies — three failures free, then 1s doubling to a fifteen-minute cap — rather than a hard lock. Counted against a keyed digest of the number rather than the user, because a backoff that skipped unregistered numbers would answer the fourth attempt with 429 for a customer and 401 for a stranger: a membership oracle cheaper than the ~6 ms timing difference FR-2.2 was written to close. Keyed rather than plain, since the column records numbers belonging to nobody and nine digits do not survive an unkeyed digest. Checked before the password so an attacker cannot spend an argon2 hash per request. Six tests, three mutations: removing the check, restricting the count to registered numbers, and dropping the cap each turn a different set red. |
| P-16 | Day 3 | **Access tokens outlived revocation by up to fifteen minutes**, including the attacker's: family revocation reaches refresh tokens at once, but a JWT carries its own authority and nothing consults the database to use one. | Closed with a `tokensValidAfter` timestamp on the user, set when reuse is detected, checked by `requireCurrentSession` on the routes that move money. Scoped deliberately: putting the check everywhere adds a database read to `/me`, the balance and history — the calls a client makes constantly — to shorten a window that only matters where something is irreversible. FR-2.6 keeps stating the bound; this closes it where the bound costs money. Five tests, two mutations. The first attempt folded the stamp into `#revokeFamily`, so an ordinary logout signed the user out of every device — a family is one device, this column is all of them — and a test caught it. |
| P-8 | Day 2 | **Idempotency keys shared one global namespace.** `idempotency_records.key` was the whole primary key and `transfers.idempotencyKey` carried its own global unique constraint, so one client could occupy a value another was about to use and turn their transfer into a 409. | Nothing was ever disclosed — the service already refused to replay a record owned by someone else — so the residual was a denial of service on the money path, reachable by any client that reuses a fixed key. Both constraints are now scoped: `@@id([userId, key])` on the record, `@@unique([initiatedBy, idempotencyKey])` on the transfer. The transfer needed an owner column, because neither account is one: a P2P transfer belongs to its sender, a top-up to its *recipient*, since the money leaves the treasury. §9.1 and §9.5 updated to match. Two tests; the mutation restoring the global lookup fails the one that matters, and Postgres refuses to recreate the old index against the data the new behaviour produces. |
| P-31 | F3 | **The integration tests wrote into the database the application was then demonstrated against, and cleaned nothing up.** Found by running F3 in a browser: the rates widget showed `11900.00` dated `2026-08-29` — a fixture from `rates.test.ts`, served to a real session hours later. The same sharing had left 3 440 accounts and 2 101 transfers, which is what made the I-4 invariant check time out at B4. | Closed by giving the suites their own database: `TEST_DATABASE_URL`, applied in `test/setup.ts` to both `DATABASE_URL` and its unpooled twin — redirecting only the first would send migrations to the database the tests no longer read, and the symptom of that is "no pending migrations to apply", which reads as success. A fallback rather than a requirement: unset, nothing changes, and CI leaves it unset because a throwaway container has nothing to protect. Proven by pointing it at a database that does not exist and watching the suites follow it there. The second half — fixtures being indistinguishable from real rows once written — stays open under P-26, which is the same problem in production. |
| P-30 | B4 | **FR-7.2's "last known value" did not survive a restart.** The rates cache was a field on `RatesService`, empty on every cold start — and P-27 records that a free-tier instance sleeps, which makes cold starts routine. An instance waking while cbu.uz was unreachable had no fallback and answered `503`, which is honest but is not what FR-7.2 promises. | Closed the same day it was raised, once the schema change was approved. `rates_snapshots` holds one row — the primary key gives uniqueness per value, so a CHECK states the "exactly one" part to the database rather than trusting the repository to. The payload is validated on the way **out**: our own older data is still data of unknown shape, and a bad row is a cache miss rather than a 500 raised at the edge after the work is done. Store failures cannot break the endpoint in either direction — an unreadable store is an empty one, and a failed write still returns the value already in hand. Four mutations; the one that removed the cold-start read turned the restart test red, which is the test that did not exist before. A second instance now reuses the first's reading, which the in-process cache could not. |
| P-20 | Day 5 | FR-4.9's lookup cap keyed on `userId`, and identities were free at ~54 ms each. | Registration and login now carry a 20-per-15-minutes budget per IP on top of the global 300, so the per-user counter has something scarce to count. The residual is P-11, which is why that entry moved to Tier A. |
